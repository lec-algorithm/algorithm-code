# Changelog

이 저장소(강의 예제 코드)의 변경 기록. 형식은
[Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따른다.

주제는 진도에 맞춰 추가된다. 새 주제가 올라오면 여기에 적는다.

## [1.3.0] - 2026-09-01

### Added

- **주제 03의 예제 두 개**: 병합 정렬·카운팅 정렬.
  `src/topic-03-divide-conquer/`에 각각 의사코드와 C·Python 구현이 들어
  있다. 병합 정렬은 분할 정복의 대표 예로 비교·이동 횟수를 세고, 카운팅
  정렬은 비교 없이(0회) 정렬해 비교 기반 정렬 하한 Ω(n log n)의 전제를
  보여준다.
- **주제 04의 예제 세 개**: 랜덤·퀵 정렬·퀵 선택.
  `src/topic-04-quick-sort/`에 각각 들어 있다. 두 언어가 같은 수열을 내는
  minstd 난수 생성기를 직접 구현해 퀵 정렬의 피벗 선택에 쓰고, 같은
  파티션으로 k번째 값만 찾는 퀵 선택으로 정렬 없이 답을 낸다.
- **주제 05의 예제 하나**: 1-sum·2-sum·3-sum 연산 세기.
  `src/topic-05-complexity/`에 들어 있다. 실행 시간 대신 대표 연산(배열
  접근)을 세어 배가 실험으로 차수를 확인하고, 실제 시간으로 재는
  `doubling_time.py`도 같이 들어 있다.

## [0.2.0] - 2026-09-01

### Added

- **주제 02의 예제 네 개**: 선택 정렬·삽입 정렬·셸 정렬·버블 정렬.
  `src/topic-02-basic-sorting/`에 각각 의사코드와 C·Python 구현이 들어 있다.
  네 알고리즘 모두 비교·이동 횟수를 같은 방식으로 세어(스왑은 임시 변수를
  거치는 이동 세 번) 한 표에서 비교할 수 있다. 셸 정렬에는 원소 수가
  커져야 간격 줄이기가 이득을 보는 지점을 보여 주는 벤치마크 스크립트가
  같이 들어 있다.

## [0.1.0] - 2026-09-01

### Added

- **주제 01의 예제 세 개**: 순차 검색·이분 검색·피보나치.
  `src/topic-01-intro-search/`에 각각 의사코드와 C·Python 구현이 들어 있다.
  이분 검색은 순차 검색과 비교 횟수를 나란히 세고, 피보나치는 재귀 호출
  횟수를 세어 재귀와 반복을 대조한다.
- 예제 하나가 폴더 하나인 구조를 정했다. 같은 폴더의 `.c`가 함께 빌드되므로
  한 폴더에 `main`은 하나만 둔다.

### Removed

- `algorithm-env`에서 온 버블 정렬 데모(`src/main.*`·`src/sort.*`·`tests/`).
  환경이 도는지 보여 주는 예제였고, 이 저장소에는 필요 없다. 그에 따라
  `Makefile`의 `run`·`test`·`debug` 타겟과 관련 작업도 뺐다. 이 저장소에는
  고정된 진입점이 없고, 실행은 열려 있는 파일을 대상으로 한다.

- 저장소 생성. [algorithm-env](https://github.com/lec-algorithm/algorithm-env)
  v1.0.0 template에서 만들었다.
- 실습 환경을 `algorithm-env` v1.0.3에 맞췄다. VS Code의 빌드·디버그 설정
  (`.vscode/`), `make debug`·`%.out` 규칙, 파일 하나만 디버그하는 구성,
  Code Runner의 ▶ 버튼이 들어왔다.
